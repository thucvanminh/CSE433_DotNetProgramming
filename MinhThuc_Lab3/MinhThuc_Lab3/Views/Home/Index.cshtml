@model List<Carousel>
@{
    ViewData["Title"] = "Home Page";
}
<div class="container">
    <h2>Danh sách sách</h2>
    @await Html.PartialAsync("_Carousel", Model)
    <!-- Nội dung sách (từ Practice Assignment 3) -->
    <div class="row">
        <!-- Thêm mã hiển thị sách nếu cần -->
    </div>
</div>